version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    pre_install:
      - pip install uv
    install:
      - uv pip install --system -e ".[docs]"

sphinx:
  configuration: docs/source/conf.py
